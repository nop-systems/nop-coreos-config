ref: nop/${basearch}/coreos/${stream}

variables:
  stream: stable
  prod: true

releasever: 39

packages:
  - xe-guest-utilities-latest
  - kitty-terminfo
  - vim
  - jq
  - bat
  - zabbix-agent2

repos:
  - zabbix

include: manifests/fedora-coreos.yaml
