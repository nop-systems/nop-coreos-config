ref: nop/${basearch}/coreos/${stream}
rojig:
  license: MIT
  name: nop-coreos
  summary: nop.systems CoreOS ${stream}

variables:
  stream: stable
  prod: true

releasever: 39

include: manifests/fedora-coreos.yaml
